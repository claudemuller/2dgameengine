#ifndef CAMERA_FOLLOW_COMPONENT_H
#define CAMERA_FOLLOW_COMPONENT_H

#include <glm/glm.hpp>

struct CameraFollowComponent {
	CameraFollowComponent() = default;
};

#endif // CAMERA_FOLLOW_COMPONENT_H
